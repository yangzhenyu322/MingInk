# Tomcat
server:
  port: 8082

# Spring
spring:
  application:
    # 应用名称
    name: mingink-system
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher # swagger路径匹配方式
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 223.82.75.76:8848
        username: lovelumine
        password: lovelumine47
        namespace: bcc846fe-65f3-4783-84a7-ed5c8f69495b
      config:
        # 配置中心地址
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        username: ${spring.cloud.nacos.discovery.username}
        password: ${spring.cloud.nacos.discovery.password}
        namespace: ${spring.cloud.nacos.discovery.namespace}
        # 配置文件格式
        file-extension: yml
        extension-configs[0]:
          data-id: mingink-mysql.yml
          refresh: true
        extension-configs[1]:
          data-id: mingink-redis.yml
          refresh: true
        # 共享配置
        shared-configs:

# Mybatis-Plus
mybatis-plus:
  configuration:
    # 将数据库中含有下划线字段映射成驼峰命名法
    map-underscore-to-camel-case: true
  # 指定类型别名的包路径
  type-aliases-package: com.mingink.system.api.domain.entity

# BaiDuYun
baidu:
  accessKeyId: ALTAKgIm9DHzDPsRcLjVycygfT
  secretAccessKey: b9e34bc5c6a04204813857cfe7d8c3b2
  # SMS 服务
  sms:
    signatureId: sms-sign-HKpwwl39058
    template: sms-tmpl-btFhKG25676
    endpoint: http://smsv3.bj.baidubce.com